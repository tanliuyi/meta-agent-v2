# Browser-facing API base. Keep this same-origin unless the marketplace server enables CORS.
VITE_MARKETPLACE_API_BASE_URL=/marketplace-api

# Development-only upstream used by the Vite proxy.
MARKETPLACE_PROXY_TARGET=http://127.0.0.1:4317
